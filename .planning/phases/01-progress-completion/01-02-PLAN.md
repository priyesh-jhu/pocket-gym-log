---
phase: 01-progress-completion
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/App.jsx
  - src/screens/ProgressScreen.jsx
  - src/screens/ProgressScreen.css
  - src/ProgressDashboard.jsx
  - src/ProgressDashboard.css
  - src/MuscleHeatmap.jsx
  - src/components/Sheet.jsx
  - src/components/Sheet.css
  - src/design/tokens.css
autonomous: true
requirements:
  - PROG-01
  - PROG-02
  - PROG-03
user_setup: []
must_haves:
  truths:
    - "E5 — Body heatmap: the group renders documented no-mapped-data and full-coverage copy, a static loading placeholder, isolated retryable calculation error, populated front/back map and labeled legend, and honest partial coverage without fabricated values; zero/one/many priority and gap items wrap without horizontal page overflow."
    - "E6 — Muscle guidance Sheet: loading/save failures retain the modal and recoverable controls; empty recent history and verified suggestions use exact copy; partial/populated guidance preserves available 1–40 targets, progress, recent mapped history, equipment/recent-exercise prioritization, custom-exercise caveat, readiness cautions, metadata, and Add exercise callbacks; zero/one/many muscles/suggestions label correctly, long copy wraps, content scrolls vertically, and focus/back/close behavior is modal and recoverable."
    - "E7 — Balance: zero-data, loading, and isolated error states never fabricate a 50/50 ratio; supported partial/populated push/pull percentages precede zero/one/many stable muscle-group rows on common 100% scales, and methodology/labels wrap in the Card."
    - "E8 — Customize dashboard Sheet: exactly four rows always remain recoverable, all-hidden uses documented copy, failed saves retain prior confirmed settings and keep the Sheet open, boundary reorder controls stay visible/disabled/announced, repeated switches and reorders are idempotent, zero/one/many visible groups are supported, and long labels wrap without shrinking targets."
    - "Selecting a heatmap region or Review muscle guidance opens the same focused Sheet; when no region is selected Review chooses the first missed muscle, and all SVG meanings have named non-SVG alternatives."
    - "Body and Balance remain subordinate members of the saved four-group order; customization changes only visibility/order and never recalculates analytics, mutates sessions, resets range, or discards content."
    - "PROG-02 adjacency: visibility/order affect presentation only; range, target, selected exercise/metric/muscle, calendar page, sessions, and analytics semantics remain independent."
    - "PROG-02 empty: all-hidden, zero missed muscles, empty history, no verified suggestions, no mapped Body data, and zero Balance data remain actionable and honest."
    - "PROG-02 ordering: four customization rows follow normalized saved order; verified suggestions retain existing deterministic direct/supporting and equipment/recent priority."
    - "PROG-02 idempotency: repeating the same range, switch, target, and boundary reorder actions does not duplicate IDs, callbacks, suggestions, or persistence writes with a different semantic result."
    - statement: "PROG-02 concurrency flat-scalar backstop: preference events derive from the latest confirmed settings ref; a failed/interrupted local save leaves prior settings visible and retryable rather than applying stale concurrent changes."
      verification: backstop
  artifacts:
    - path: src/ProgressDashboard.jsx
      provides: "Complete BodyHeatmapGroup, muscle guidance, and BalanceGroup slices"
      contains: "Muscle guidance"
    - path: src/MuscleHeatmap.jsx
      provides: "Theme-role-driven front/back SVG with named selectable regions"
      contains: "data-muscle"
    - path: src/components/Sheet.jsx
      provides: "Shared modal focus, close-label, focus-return, and optional history-back support used by both Progress sheets"
      contains: "aria-modal"
    - path: src/design/tokens.css
      provides: "Central light/dark muscle visualization roles if existing semantic roles are insufficient"
      contains: "--muscle-empty"
  key_links:
    - from: src/ProgressDashboard.jsx
      to: src/MuscleHeatmap.jsx
      via: "BodyHeatmapGroup passes existing coverage/set scores, selected muscle, and open callback"
      pattern: "<MuscleHeatmap"
    - from: src/ProgressDashboard.jsx
      to: src/components/Sheet.jsx
      via: "MuscleGuidanceSheet uses the shared modal primitive"
      pattern: "Muscle guidance"
    - from: src/screens/ProgressScreen.jsx
      to: src/components/Sheet.jsx
      via: "screen-owned Customize dashboard uses the shared modal primitive"
      pattern: "Customize dashboard"
    - from: src/ProgressDashboard.jsx
      to: src/stats.js
      via: "muscleHeatmapCoverage, muscleSetVolume, musclePriorities, exerciseSuggestionsForMissed, muscleBalance, and pushPullRatio remain unchanged"
      pattern: "exerciseSuggestionsForMissed"
    - from: src/MuscleHeatmap.jsx
      to: src/design/tokens.css
      via: "SVG style values reference semantic muscle roles instead of Progress literals"
      pattern: 'var\(--muscle-'
  prohibitions:
    - id: PROHIB-PROG-NONMEDICAL
      requirement: PROG-01
      category: safety
      status: resolved
      verification: judgment
      enforcement: unverified-prohibition
      statement: "Guidance must not present readiness, missed muscles, imbalance, soreness, or plateau signals as a medical diagnosis; retain qualified-advice and trend-based non-medical wording."
    - id: PROHIB-PROG-NO-DATA-MUTATION
      requirement: PROG-02
      category: safety
      status: resolved
      verification: judgment
      enforcement: unverified-prohibition
      statement: "Dashboard customization and guidance must not mutate workout records, rewrite analytics semantics, or imply that Add exercise has already saved a workout."
---

<objective>
As a workout tracker user, I want body guidance and balance analytics to be usable, honest, and accessible inside the four-group dashboard, so that I can understand gaps without the app changing my history or presenting medical conclusions.

Purpose: Expand the tracer with the complete Body heatmap/guidance and Balance vertical slices, then harden the two shared preference-driven Sheets.
Output: Tokenized interactive anatomy, recoverable muscle guidance, honest Balance analytics, and idempotent accessible customization.
</objective>

<execution_context>
@/Users/pagraw15/.codex/gsd-core/workflows/execute-plan.md
@/Users/pagraw15/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-progress-completion/01-UI-SPEC.md
@.planning/phases/01-progress-completion/01-01-SUMMARY.md
@../docs/superpowers/specs/2026-08-14-android-redesign-design.md
@.superpowers/sdd/2026-08-14-android-redesign-1-foundation/progress.md
@src/screens/ProgressScreen.jsx
@src/ProgressDashboard.jsx
@src/ProgressDashboard.css
@src/MuscleHeatmap.jsx
@src/components/Sheet.jsx
@src/components/Sheet.css
@src/design/tokens.css
@src/stats.js
@src/data/formGuide.js
</context>

<threat_model>
ASVS L1 / block-high review: this remains presentation-only and introduces no new external input, credential, cloud, or authorization boundary. Existing sessions and readiness notes are sensitive fitness data, so do not log them, copy them into a new store, or expose them outside the current local render. Preserve risks are more relevant than new attack surface: malformed owned data must fail within one group; weekly target input must be numerically clamped to 1–40; `onAddExercise` must receive only an existing verified exercise name; modal focus/back state must not strand a user; customization must never write session data. No new high-risk security finding is accepted, and global storage/Firebase hardening remains outside this presentation phase.
</threat_model>

## Artifacts this phase produces

- New files: none.
- New/expanded symbols in `src/ProgressDashboard.jsx`: `MuscleGuidanceSheet`, `MuscleLegend`, and the completed `BodyHeatmapGroup`/`BalanceGroup` exports.
- New additive props in the existing `Sheet` symbol: `closeLabel = "Close"`, `initialFocusRef = null`, `returnFocusRef = null`, and `dismissOnHistory = false`; Progress opts into labels/focus/history behavior while completed Session callers retain all defaults.
- New token symbols only if needed for hue-distinct non-semantic anatomy bands: `--muscle-low`, `--muscle-moderate`, `--muscle-high`, and `--muscle-review`, each defined in both light and dark token blocks.
- New CSS class families: `progress-body-*`, `progress-guidance-*`, `progress-balance-*`, and scoped Sheet state/focus classes.

<tasks>

<task type="auto">
  <name>Task 1: Complete Body heatmap and modal muscle guidance</name>
  <reversibility rating="costly">The shared Sheet focus/history contract serves Progress and existing session flows, so undo must coordinate all callers; opt-in props keep the current default interface compatible.</reversibility>
  <files>src/screens/ProgressScreen.jsx, src/screens/ProgressScreen.css, src/ProgressDashboard.jsx, src/ProgressDashboard.css, src/MuscleHeatmap.jsx, src/components/Sheet.jsx, src/components/Sheet.css, src/design/tokens.css</files>
  <read_first>src/App.jsx, src/screens/ProgressScreen.jsx, src/screens/ProgressScreen.css, src/ProgressDashboard.jsx, src/ProgressDashboard.css, src/MuscleHeatmap.jsx, src/components/Sheet.jsx, src/components/Sheet.css, src/design/tokens.css, src/stats.js, src/data/formGuide.js, .planning/phases/01-progress-completion/01-UI-SPEC.md, ../docs/superpowers/specs/2026-08-14-android-redesign-design.md, .superpowers/sdd/2026-08-14-android-redesign-1-foundation/progress.md</read_first>
  <action>Finish `BodyHeatmapGroup` using the existing `muscleHeatmapCoverage`, `muscleSetVolume`, `musclePriorities`, `muscleCoverageGaps`, `exerciseSuggestionsForMissed`, `MUSCLES`, and `formGuide` results without formula changes. Use `SegmentedButtons` for Coverage/Set volume. Preserve front/back geometry, current range dates, labeled legend, priorities, gaps, and custom-exercise caveat inside this one group. Refactor `MuscleHeatmap` so every fill/stroke is applied through its existing style/paint path with semantic CSS variables; keep literal colors only in central tokens, define any new muscle band role in both themes, and keep Home freshness behavior unchanged. Make each selectable region expose programmatic muscle name and value/state plus a visible selected outline; add an equivalent named 48px chip/list route so SVG is never the sole control. Use exact no-mapped/full-coverage/needs-attention copy. Rename the Sheet to `Muscle guidance` and implement every required label/action/accessibility name. Review chooses the first missed muscle when none is selected; region selection opens the same Sheet focused on that muscle. Preserve target progress and 1–40 editing, recent mapped history, deterministic verified Direct/Supporting suggestions, equipment/recent prioritization, Add callback, empty copy, pain/high-soreness cautions, qualified advice, and the custom-exercise caveat. Enhance `Sheet` additively with exact optional props `closeLabel = "Close"`, `initialFocusRef = null`, `returnFocusRef = null`, and `dismissOnHistory = false`. Implement focus entry/trap/return and opt-in history dismissal behind those props; Progress Muscle guidance passes `Close muscle guidance`, meaningful refs, and `dismissOnHistory`, while every existing App/Session Sheet call omits them and retains current Escape/scrim/default-close behavior. Gesture dismissal remains optional and never the only exit. Do not change completed Session flow, history sentinel, or default Sheet behavior.</action>
  <verify>Run `npm test` and `npm run build`; source-assert that `MuscleHeatmap.jsx` contains no hex/rgb/hsl literal, weekly target input retains `min="1"` and `max="40"`, all required muscle guidance labels/copy/accessibility names are present, `Sheet` declares the exact four backward-compatible defaults, Progress alone opts into `dismissOnHistory`, and existing App/Session Sheet call sites remain prop-compatible and omit the opt-in props.</verify>
  <acceptance_criteria>
    - Coverage and Set volume use the unchanged existing data and geometry, render honest no-data/partial/full states, and carry meaning through labels/numbers/outlines as well as hue.
    - A pointer, touch, keyboard, or equivalent-list muscle selection opens one modal Sheet focused on the same muscle and returns focus when closed.
    - The additive Sheet props default exactly as specified; all existing App/Session call sites omit them and preserve completed Session behavior.
    - Weekly targets clamp to 1–40; repeated entry of the same value is idempotent; failed persistence retains the prior confirmed target and Sheet state.
    - Recent history, readiness cautions, verified suggestions, metadata, custom caveat, and Add callback from the Claude ledger all remain available.
    - Guidance never diagnoses; pain copy recommends avoiding painful movement and qualified advice if pain persists.
  </acceptance_criteria>
  <done>Body coverage/set volume and the full muscle-guidance workflow are tokenized, accessible, recoverable, and behavior-preserving.</done>
</task>

<task type="auto">
  <name>Task 2: Complete honest push/pull and muscle-group Balance</name>
  <files>src/screens/ProgressScreen.jsx, src/screens/ProgressScreen.css, src/ProgressDashboard.jsx, src/ProgressDashboard.css</files>
  <read_first>src/screens/ProgressScreen.jsx, src/screens/ProgressScreen.css, src/ProgressDashboard.jsx, src/ProgressDashboard.css, src/stats.js, src/charts/chartTheme.js, .planning/phases/01-progress-completion/01-UI-SPEC.md, .superpowers/sdd/2026-08-14-android-redesign-1-foundation/progress.md</read_first>
  <action>Finish `BalanceGroup` as one filled Card. Preserve `pushPullRatio(list, rangeDays)` and `muscleBalance(list, Math.max(1, Math.ceil(rangeDays / 7)))`. Put explicit push/pull percentages, text labels, a two-part 0–100 track, and exact methodology copy first. When mapped push+pull is zero, render the exact empty copy and no fabricated track split; when data exists ensure displayed percentages total 100. Follow with stable group rows using group name, numeric percent, and a common 100% track. Use a restrained consistent role set rather than unrelated rainbow colors, and ensure push/pull/group meaning remains distinct in monochrome through labels and layout. Preserve zero, one, many, partial, loading, and isolated retryable error behavior without hiding other groups. Do not alter stats.js or add balance heuristics.</action>
  <verify>Run `npm test` and `npm run build`; source-assert exact methodology and zero-state strings, a 100% common scale, and no literal/fabricated `50`/`50` fallback in Balance rendering.</verify>
  <acceptance_criteria>
    - Push/pull appears before group rows and totals 100 only when mapped data exists.
    - Zero data displays `Log mapped push and pull exercises to see your ratio.` and does not render a 50/50 split.
    - Group rows preserve existing stable analytics order, numeric percentages, and proportional common-scale bars.
    - Long labels/methodology wrap, and colors are supportive rather than the only differentiator.
  </acceptance_criteria>
  <done>Balance preserves the existing group analytics plus push/pull in one readable, correctly empty-aware group.</done>
</task>

<task type="auto">
  <name>Task 3: Harden Customize ordering, save failure, and modal interaction</name>
  <files>src/App.jsx, src/screens/ProgressScreen.jsx, src/screens/ProgressScreen.css, src/ProgressDashboard.css, src/components/Sheet.jsx, src/components/Sheet.css</files>
  <read_first>src/App.jsx, src/screens/ProgressScreen.jsx, src/screens/ProgressScreen.css, src/ProgressDashboard.css, src/components/Sheet.jsx, src/components/Sheet.css, src/equipmentPrefs.js, .planning/phases/01-progress-completion/01-UI-SPEC.md, .planning/phases/01-progress-completion/01-01-SUMMARY.md</read_first>
  <action>Complete the screen-owned `Customize dashboard` Sheet with exact supporting copy and exactly four labeled switch rows in current saved order: e1RM progression, Daily trend, Body heatmap, Balance. Keep both Move {group} up/down controls visible; disable and announce only true boundaries. Use the latest confirmed settings ref from Plan 01-01 for every switch/reorder/range/target update so rapid events compose instead of overwriting one another. Skip a persistence call when normalized output is semantically unchanged. On synchronous local save failure, retain the prior confirmed settings, keep the Sheet open, focus/announce the documented polite error, and allow retry. Reordering must affect only render order; visibility must reveal/hide complete groups with all subordinate content. Verify hiding all four leaves the toolbar and exact all-hidden heading/body/action; re-enabling any row restores that group's prior transient selection when it still exists. Give the Sheet exact close label, focus trapping/return, Escape/scrim/history-back support, 48px switches/reorder controls, and wrapping rows at 360px. Do not add confirmation UI because Progress has no destructive actions.</action>
  <verify>Run `npm run build`; source-assert exactly four group labels, both `Move ${...} up/down` accessible names, exact all-hidden/save-error copy, visible disabled boundary controls, and no call path from customization to session mutation functions.</verify>
  <acceptance_criteria>
    - Exactly four rows are shown even when all four groups are hidden; Customize is never hidden.
    - Same-value operations are idempotent and rapid operations preserve the latest confirmed stable order without duplicates.
    - Interrupted/failed device persistence visibly retains prior confirmed range/visibility/order/target and leaves controls retryable.
    - Reorder/hide does not reset range, exercise, metric, selected muscle, calendar page, workout sessions, or analytics results.
    - Modal keyboard/back/scrim/close behavior and focus return work for both Customize and Muscle guidance.
  </acceptance_criteria>
  <done>Dashboard customization is accessible, stable under repeated/rapid actions, recoverable on save failure, and presentation-only.</done>
</task>

</tasks>

<verification>

- [ ] `npm test` passes without changing pure analytics tests/formulas.
- [ ] `npm run test:tz` passes in all configured time zones.
- [ ] `npm run build` succeeds.
- [ ] `npm run lint` adds no Progress/Sheet/MuscleHeatmap findings; unrelated Phase 3 legacy cleanup remains out of scope.
- [ ] Source assertions prove target bounds 1–40, all exact Body/Balance/Sheet copy, token-only SVG colors, and no workout-data mutation from customization.

</verification>

<success_criteria>

- All tasks completed and verification checks pass.
- Body, muscle guidance, Balance, and customization preserve every capability named in the Claude ledger and UI-SPEC.
- No medical diagnosis framing, workout mutation, new dependency, new persistence shape, or global legacy cleanup is introduced.
- PROG-01/02/03 are satisfied for these vertical slices in both themes through existing tokens and chart hooks.

</success_criteria>

<output>
After completion, create `.planning/phases/01-progress-completion/01-02-SUMMARY.md`.
</output>
