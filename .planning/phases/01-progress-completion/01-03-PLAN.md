---
phase: 01-progress-completion
plan: "03"
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - scripts/verify-lint-baseline.mjs
  - src/App.jsx
  - src/screens/ProgressScreen.jsx
  - src/screens/ProgressScreen.css
  - src/ProgressDashboard.jsx
  - src/ProgressDashboard.css
  - src/MuscleHeatmap.jsx
  - src/components/Sheet.jsx
  - src/components/Sheet.css
  - src/design/tokens.css
  - src/charts/chartTheme.js
  - src/charts/useThemeTokens.js
autonomous: false
requirements:
  - PROG-01
  - PROG-02
  - PROG-03
user_setup: []
must_haves:
  truths:
    - "E9 — Whole-screen states: hydration renders static tonal skeletons and `Loading progress…` without flashing empty content; no-session empty, global calculation error, preference-save error, and all-hidden surfaces use exact actionable UI-SPEC copy; global replacement occurs only when no analytics can safely derive; all state text wraps at 200% without horizontal page scrolling."
    - "All E1–E8 controls and group-local states use one semantic token system, the four allowed Progress type roles (Label/Body/Title/Headline), weights 400/600, tabular numeric values, standard 4/8/16/24/32/48/64 spacing only, 48px targets, visible focus, and no screen color literal or `!important` workaround."
    - "Theme changes repeatedly rerender every Recharts grid/axis/tooltip/series through useThemeTokens and every own-SVG/body/balance surface through CSS roles without reload, stale colors, or losing range/exercise/metric/muscle/calendar/order selections."
    - "Charts remain understandable without color or tooltip: e1RM and Daily expose text/data representations, Body exposes labels/legend/selection/Sheet equivalents, and Balance exposes labels/numbers/common scales."
    - "At 360px and 390px in light and dark themes, the screen is one narrow column; toolbar/selectors wrap before controls shrink, long content supports 200% zoom, page overflow never becomes horizontal, and only the calendar has a labeled contained horizontal scroller."
    - "Reduced motion disables Recharts entry animation, skeleton shimmer, Sheet drag-settle, and group reorder animation; routine range/selection updates do not jump scroll or produce noisy announcements."
    - "All existing Progress capabilities recorded in the Claude progress ledger remain present after style consolidation, including e1RM, all insights/evidence, daily metrics, range summary, planned days, calendar/history/details, heatmap modes, priorities/gaps, targets, cautions, suggestions/Add, Balance, and push/pull."
    - "PROG-03 idempotency: repeated System/Light/Dark toggles produce the same palette for the same final data-theme and require no page reload."
    - statement: "PROG-03 concurrency flat-scalar backstop: MutationObserver resolves the latest root data-theme on each notification; chart renders and CSS variables converge on the final theme without retaining an earlier palette object."
      verification: backstop
  artifacts:
    - path: scripts/verify-lint-baseline.mjs
      provides: "Deterministic ESLint API gate allowing at most the one recorded App guest-bootstrap finding and rejecting every additional result"
      contains: "react-hooks/set-state-in-effect"
    - path: src/screens/ProgressScreen.jsx
      provides: "Complete state matrix, group isolation, reduced-motion/chart behavior, and four-group accessible screen landmark"
      contains: "progress-title"
    - path: src/screens/ProgressScreen.css
      provides: "Single adjacent tokenized Progress stylesheet for all groups, states, sheets, and responsive behavior"
      contains: "@media (max-width: 390px)"
    - path: src/charts/useThemeTokens.js
      provides: "Live chart-palette rerender on data-theme changes"
      contains: "MutationObserver"
    - path: src/charts/chartTheme.js
      provides: "Only permitted literal colors consumed by Recharts"
      contains: "CHART_THEMES"
  key_links:
    - from: src/screens/ProgressScreen.jsx
      to: src/charts/useThemeTokens.js
      via: "all Recharts groups consume the live theme palette"
      pattern: "useThemeTokens"
    - from: src/ProgressDashboard.jsx
      to: src/screens/ProgressScreen.css
      via: "all non-dynamic Progress presentation uses screen-adjacent classes"
      pattern: "progress-"
    - from: src/MuscleHeatmap.jsx
      to: src/design/tokens.css
      via: "own SVG fill/stroke use CSS semantic roles and update without JS reload"
      pattern: 'var\(--muscle-'
    - from: src/App.jsx
      to: src/screens/ProgressScreen.jsx
      via: "loading and retry/navigation callbacks select exact whole-screen states"
      pattern: "loading"
  prohibitions:
    - id: PROHIB-PROG-SILENT-REMOVAL-FINAL
      requirement: PROG-03
      category: transparency
      status: resolved
      verification: judgment
      enforcement: unverified-prohibition
      statement: "Theme and layout consolidation must not silently remove or visually conceal existing analytics, controls, detail fields, warnings, or actions."
---

<objective>
As a workout tracker user, I want Progress to remain clear and stable across loading, empty, failure, light/dark, narrow-screen, keyboard, and reduced-motion conditions, so that the completed redesign is dependable rather than only attractive on the happy path.

Purpose: Close the state, theme, accessibility, and responsive contracts after the four functional slices, then obtain the one final human visual verification checkpoint.
Output: A Progress-local tokenized presentation, isolated state matrix, automated quality/source evidence, and human approval at 360/390 light/dark.
</objective>

<execution_context>
@/Users/pagraw15/.codex/gsd-core/workflows/execute-plan.md
@/Users/pagraw15/.codex/gsd-core/templates/summary.md
@/Users/pagraw15/.codex/gsd-core/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-progress-completion/01-UI-SPEC.md
@.planning/phases/01-progress-completion/01-01-SUMMARY.md
@.planning/phases/01-progress-completion/01-02-SUMMARY.md
@../docs/superpowers/specs/2026-08-14-android-redesign-design.md
@.superpowers/sdd/2026-08-14-android-redesign-1-foundation/progress.md
@src/App.jsx
@src/screens/ProgressScreen.jsx
@src/screens/ProgressScreen.css
@src/ProgressDashboard.jsx
@src/ProgressDashboard.css
@src/MuscleHeatmap.jsx
@src/components/Sheet.jsx
@src/design/tokens.css
@src/design/base.css
@src/charts/chartTheme.js
@src/charts/useThemeTokens.js
@src/index.css
</context>

<threat_model>
ASVS L1 / block-high review: state surfaces, CSS, chart palette observation, and manual viewport verification create no new trust boundary and no new high-risk security exposure. Preserve risks are inadvertent disclosure of notes through logging, malformed persisted data escalating one failed group to an app-wide crash, focus escaping a modal, stale theme state obscuring meaningful status, or Progress-local cleanup deleting functionality. Keep runtime errors user-readable without logging session payloads, isolate groups before global fallback, use semantic non-color indicators, and compare the final implementation against the ledger capability inventory. Do not perform security hardening, global legacy cleanup, deployment, publishing, or Android release work in this phase.
</threat_model>

## Artifacts this phase produces

- New file: `scripts/verify-lint-baseline.mjs`, a checked-in deterministic ESLint JSON/API gate grounded in the recorded App guest-bootstrap concern.
- New symbols in `src/screens/ProgressScreen.jsx`: `ProgressGroupBoundary` and `useReducedMotion` (or equivalently named local symbols with the same stated contracts).
- Consolidated CSS artifact: all Progress-specific styles move into existing `src/screens/ProgressScreen.css`; `src/ProgressDashboard.css` is removed only after its selectors are migrated and source assertions prove no orphaned class names.
- No new analytics, persistence, chart, domain, or data-model symbols.

## Source traceability audit built into execution

- E1 is implemented by Plan 01-01 Task 1 and verified again in this plan's state/theme pass.
- E2 is implemented by Plan 01-01 Task 2.
- E3 and E4 are implemented by Plan 01-01 Task 3.
- E5 and E6 are implemented by Plan 01-02 Task 1.
- E7 is implemented by Plan 01-02 Task 2.
- E8 is implemented by Plan 01-02 Task 3.
- E9 is implemented by this plan Task 1.
- Claude Progress section and ledger slices 1–6 are audited in this plan Task 2; foundation/Home/Session/Settings and future History/Weight/global cleanup are explicitly not replanned.

<tasks>

<task type="auto">
  <name>Task 1: Complete whole-screen and isolated group state contracts</name>
  <files>src/App.jsx, src/screens/ProgressScreen.jsx, src/screens/ProgressScreen.css, src/ProgressDashboard.jsx, src/components/Sheet.jsx, src/components/Sheet.css</files>
  <read_first>src/App.jsx, src/screens/ProgressScreen.jsx, src/screens/ProgressScreen.css, src/ProgressDashboard.jsx, src/components/Sheet.jsx, src/components/Sheet.css, .planning/phases/01-progress-completion/01-UI-SPEC.md, .planning/phases/01-progress-completion/01-01-SUMMARY.md, .planning/phases/01-progress-completion/01-02-SUMMARY.md</read_first>
  <action>Finish the state matrix with exact copy. Use a `<section aria-labelledby="progress-title">` and logical group headings. During initial active-Progress hydration keep the toolbar/group footprint and static tonal skeletons with `Loading progress…`; do not shimmer, and do not render empty first. With no sessions render `No progress yet` plus exact body and a working `Go to Home` only by passing/using the existing `switchTab("log")` callback; omit the CTA if no callback. Keep the toolbar only when customization remains meaningful. Keep all-hidden copy/action exact. Add `ProgressGroupBoundary` around each group so a render/calculation failure shows `Progress couldn’t be calculated`, the exact reassurance/review body, and `Try again` locally while unaffected groups remain; use global replacement only when settings/composition cannot safely derive any group. Reset only the affected boundary on retry. Preserve selected range/exercise/metric/muscle/calendar page/order through normal state transitions unless its source option disappeared. Use polite live regions for loading and save errors and `role="alert"` for blocking calculation failure. Keep long copy, 200% text zoom, and focus above NavBar/Sheet edges. Do not invent a DOM test suite or change pure analytics error semantics.</action>
  <verify>Run `npm run build`; source-assert every exact Loading/Empty/Error/All hidden/Preference save string from the Copywriting Contract, `aria-labelledby="progress-title"`, a per-group error boundary, polite live regions, and the absence of an inert `Go to Home` button.</verify>
  <acceptance_criteria>
    - Hydration cannot flash the empty state and uses static, reduced-motion-safe skeletons.
    - Full empty/global error/all hidden use exact actions and copy; local partial/error states do not replace unaffected groups.
    - Retry remounts only the failed group and routine transitions retain every still-valid selection.
    - Status/error text wraps at 200% and does not create horizontal page overflow.
  </acceptance_criteria>
  <done>E9 and all cross-group state transitions are recoverable, isolated where possible, exact-copy, and accessible.</done>
</task>

<task type="auto">
  <name>Task 2: Consolidate tokenized styling and prove theme/accessibility preservation</name>
  <files>scripts/verify-lint-baseline.mjs, src/screens/ProgressScreen.jsx, src/screens/ProgressScreen.css, src/ProgressDashboard.jsx, src/ProgressDashboard.css, src/MuscleHeatmap.jsx, src/components/Sheet.jsx, src/components/Sheet.css, src/design/tokens.css, src/charts/chartTheme.js, src/charts/useThemeTokens.js</files>
  <read_first>scripts/verify-lint-baseline.mjs, package.json, eslint.config.js, .planning/codebase/CONCERNS.md, src/App.jsx, src/screens/ProgressScreen.jsx, src/screens/ProgressScreen.css, src/ProgressDashboard.jsx, src/ProgressDashboard.css, src/MuscleHeatmap.jsx, src/components/Card.css, src/components/Button.css, src/components/SegmentedButtons.css, src/components/Sheet.jsx, src/components/Sheet.css, src/design/tokens.css, src/design/base.css, src/charts/chartTheme.js, src/charts/useThemeTokens.js, .planning/phases/01-progress-completion/01-UI-SPEC.md, ../docs/superpowers/specs/2026-08-14-android-redesign-design.md, .superpowers/sdd/2026-08-14-android-redesign-1-foundation/progress.md</read_first>
  <action>Complete the Progress-local presentation migration. Move every remaining non-dynamic Progress layout/type/color style object and every selector in `ProgressDashboard.css` into semantic classes in adjacent `src/screens/ProgressScreen.css`, then delete `ProgressDashboard.css` only after all imports/selectors migrate. Dynamic widths/positions derived from data may remain inline, but no hard-coded color, typography, spacing, border, background, or shadow may. Keep Recharts literal colors exclusively in `chartTheme.js`; every chart gets the live `useThemeTokens()` result so repeated data-theme mutations rerender grid, axes, tooltip, selection, and series without reload. Keep own SVG colors in CSS/token roles. Progress CSS may use spacing only from `--sp1`, `--sp2`, `--sp4`, `--sp6`, `--sp8`, or explicit 48/64 composition; remove `--sp3`, `--sp5`, and `--sp10`. Scope exactly Label `--text-label-sm`, Body `--text-body`, Title `--text-title-lg`, and Headline `--text-headline`, with 400/600 weights and tabular numerals. Account for the effective inherited rules in `Card.css`, `Button.css`, `SegmentedButtons.css`, and `Sheet.css`: add Progress-scoped overrides in `ProgressScreen.css` for primitive typography, 400/600 weights, card/button/segment/Sheet padding, and gaps so the computed Progress result uses only the four roles and allowed 4-grid spacing; do not edit primitive CSS app-wide or regress completed Home/Session/Settings consumers. Keep accent under the 60/30/10 contract and semantic warn/success/error meanings. Add/use `useReducedMotion` so Recharts `isAnimationActive` is false under reduced motion and Progress skeleton/reorder/Sheet enhancements do not animate; normal motion uses existing duration/easing roles. At 360/390 keep one column and 48px controls, stacking before compression; above 420 remain centered in `.app-shell`. Run a preservation audit against ledger slices 1–6 and explicitly account for e1RM; every insight/evidence; all daily metrics/bars; summary/planned days; 12-week paging/details; heatmap modes/priorities/gaps/targets/history/cautions/suggestions/Add; Balance/push-pull. Do not remove global legacy CSS/markup owned by Phase 3. Create checked-in `scripts/verify-lint-baseline.mjs` using the installed ESLint API, not shell text parsing: lint `.` and flatten all messages of either severity; locate the recorded baseline region in `src/App.jsx` between the `// Bootstrap` marker and the following auth-observer effect; allow zero or one finding only when its file is `src/App.jsx`, its `ruleId` is exactly `react-hooks/set-state-in-effect`, and its line falls inside that guest-bootstrap region. Fail nonzero for a second matching finding, any warning/error with another rule/path/region, and especially any finding in phase-touched files; print deterministic allowed/unexpected diagnostics. `npm run lint` may be recorded separately as raw diagnostics, but it is not the pass gate. After tests, build, the baseline-aware script, and source assertions pass, start `npm run dev -- --host 127.0.0.1 --port 5173 --strictPort` in the background, wait for readiness, and keep it running for Task 3.</action>
  <verify>Run `npm test`, `npm run test:tz`, `npm run build`, and the pass gate `node scripts/verify-lint-baseline.mjs`; the script must exit 0 with zero findings or only the single source-grounded App guest-bootstrap baseline and must exit nonzero for every additional ESLint message. Then run source assertions: (1) no hex/rgb/hsl literals in `ProgressScreen.jsx`, `ProgressDashboard.jsx`, `ProgressScreen.css`, `MuscleHeatmap.jsx`, or Progress-related Sheet additions; (2) no `!important`; (3) no `--sp3|--sp5|--sp10`; (4) effective Progress-scoped Card/Button/SegmentedButtons/Sheet rules override inherited typography, weights, padding, and gaps to the four roles/400–600/allowed 4-grid without modifying primitive CSS; (5) no import/orphan selector for deleted `ProgressDashboard.css`; (6) all Recharts components wire live theme and reduced-motion animation; (7) every ledger capability string/symbol remains reachable; (8) `curl -fsS http://127.0.0.1:5173/` succeeds before the checkpoint.</verify>
  <acceptance_criteria>
    - Light/dark changes repeatedly without reload and no chart, SVG, tooltip, axis, focus, selected, disabled, warning, or empty-state color remains stale.
    - Progress has no hard-coded screen colors, new inline style system, `!important`, banned spacing token, sub-11px text, or weight outside 400/600.
    - Computed Progress Card/Button/SegmentedButtons/Sheet typography, weights, padding, and gaps are corrected by `ProgressScreen.css` scope; shared primitive styles and completed consumers are unchanged.
    - All targets are at least 48x48, focus is visible, Sheets trap/return focus, calendar alone may horizontally scroll, and 200% text zoom does not clip.
    - Reduced motion removes chart entry, skeleton, drag-settle, and reorder animation while preserving understandable state change.
    - Source audit accounts for all three requirements, E1–E9, the Claude Progress design section, and every unfinished Progress ledger capability with no silent removal.
    - Full test/timezone/build gates pass; lint has no new touched-file finding, and the recorded App guest-bootstrap lint concern is not used as justification for global cleanup.
    - The strict-port development server is ready at `http://127.0.0.1:5173/` for the final human checkpoint.
  </acceptance_criteria>
  <done>The final Progress implementation is locally consolidated, tokenized, theme-live, reduced-motion-safe, source-audited, and behavior-complete.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify 360/390 light/dark and interaction states</name>
  <files>src/screens/ProgressScreen.jsx, src/screens/ProgressScreen.css, src/ProgressDashboard.jsx, src/MuscleHeatmap.jsx, src/components/Sheet.jsx</files>
  <read_first>.planning/phases/01-progress-completion/01-UI-SPEC.md, .planning/phases/01-progress-completion/01-01-SUMMARY.md, .planning/phases/01-progress-completion/01-02-SUMMARY.md, src/screens/ProgressScreen.jsx, src/screens/ProgressScreen.css, src/ProgressDashboard.jsx, src/MuscleHeatmap.jsx, src/components/Sheet.jsx</read_first>
  <action>Use the already-running strict-port server from Task 2. Conduct only the human visual/interaction review below; do not start another server, mutate/delete workouts, inject malformed data merely to force an error, perform Android-device verification, deploy, publish, or push.</action>
  <what-built>Completed four-group Progress destination running at `http://127.0.0.1:5173/`, with tokenized e1RM, Daily trend/calendar, Body guidance, Balance, customization, state handling, and live light/dark charts.</what-built>
  <how-to-verify>Open `http://127.0.0.1:5173/` and inspect Progress at 360px and 390px in Light and Dark using existing Settings theme controls. Verify populated four-group order; exact 7/28/90 controls; e1RM and Daily inspection; a naturally reachable zero/partial state; calendar Earlier/Later/Current and workout detail close/focus; Coverage/Set volume, muscle selection and complete guidance Sheet; zero/populated Balance where data permits; Customize reorder/boundary buttons, hide all, recovery, and re-enable; visible keyboard focus, Tab trap, Escape/scrim/browser-back close and focus return; 200% text zoom; reduced motion; no horizontal page scroll or stale theme colors. Observe reload loading/empty behavior where naturally reachable.</how-to-verify>
  <resume-signal>Type `approved` or describe the Progress visual/interaction defects to fix.</resume-signal>
  <verify>Human checks both specified widths and themes plus the listed interaction states at the supplied local URL and reports approval or concrete defects.</verify>
  <acceptance_criteria>
    - Human confirms 360px Light, 360px Dark, 390px Light, and 390px Dark are coherent and horizontally contained.
    - Human confirms both Sheets, range/customization, chart/data inspection, calendar details, keyboard focus, Escape/back/scrim close, and focus return.
    - Human confirms repeated theme/range/order interactions do not reload, jump scroll, lose still-valid selections, or show stale colors.
    - Any reported defect is fixed and the applicable automated/source checks rerun before approval.
  </acceptance_criteria>
  <done>Human types `approved`, or all reported Progress visual/interaction defects are resolved and then approved.</done>
</task>

</tasks>

<verification>

- [ ] `npm test` passes.
- [ ] `npm run test:tz` passes in all configured time zones.
- [ ] `npm run build` succeeds.
- [ ] `node scripts/verify-lint-baseline.mjs` passes, allowing zero findings or only the one source-grounded App guest-bootstrap `react-hooks/set-state-in-effect` finding and rejecting every additional message.
- [ ] Source assertions prove token/color/type/spacing/motion constraints and complete ledger capability preservation.
- [ ] Human approves 360px and 390px in both themes and the listed interaction states.

</verification>

<success_criteria>

- All tasks and the final human checkpoint complete.
- PROG-01, PROG-02, and PROG-03 are covered by executable implementation and verification steps.
- E1–E9 and every surfaced edge/prohibition are explicitly resolved or flagged for judgment review.
- The result remains compatible with the approved Claude design and completed ledger slices; no foundation/Home/Session/Settings/History/Weight/global-cleanup work is replanned.
- No deployment, publishing, push, MUI, shadcn, Tailwind, Material Web, new UI framework, new styling dependency, or data-layer change occurs.

</success_criteria>

<output>
After completion, create `.planning/phases/01-progress-completion/01-03-SUMMARY.md`.
</output>
