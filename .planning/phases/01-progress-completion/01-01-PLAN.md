---
phase: 01-progress-completion
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/App.jsx
  - src/components/SegmentedButtons.jsx
  - src/progressDashboardSettings.js
  - src/progressDashboardSettings.test.js
  - src/screens/ProgressScreen.jsx
  - src/screens/ProgressScreen.css
  - src/ProgressDashboard.jsx
  - src/ProgressDashboard.css
autonomous: true
requirements:
  - PROG-01
  - PROG-02
  - PROG-03
user_setup: []
must_haves:
  truths:
    - "E1 — Global toolbar: while sessions hydrate, the Progress shell preserves the toolbar footprint without applying stale range data; 7/28/90 options read 7 days, 28 days, and 90 days, expose Show last {N} days names, save immediately, retain the prior confirmed range with the documented live-region error if device persistence fails, stack without clipping when needed, and keep 48px targets."
    - "E2 — e1RM progression: the raised Estimated one-rep max group handles static loading, isolated error, the exact weighted-set empty copy, and populated/partial states without hiding other groups; eligible exercise names are stable alphabetical order, dates are chronological, latest/first/change and an accessible value list preserve the existing formula, unit, rounding, full-history series, complete insight list, evidence, recovery/plateau wording, and non-medical disclaimer."
    - "E3 — Daily Trend: the Daily trend group handles loading, isolated error, local empty/partial selections, zero/one/many records, wrapping controls, and exactly 7, 28, or 90 chronological daily values and bars including zero days; it preserves All exercises, Volume, Max weight, Estimated 1RM, Sessions, full date/value/unit inspection, accessible values, the range summary, and the editable planned-days target."
    - "E4 — Training calendar: the Daily trend group keeps a separately paged 12-week calendar footprint, valid Earlier/Later/Current navigation, navigable empty periods, selectable populated dates, complete available workout details, notes, exercise/set counts, correct singular/plural copy, contained labeled horizontal scrolling, and wrapped long workout text; paging never changes range-sensitive Daily/Body/Balance data."
    - "There are exactly four top-level groups in saved order — e1RM progression, Daily trend, Body heatmap, Balance — and legacy summary/calendar/trend content is consolidated under Daily trend rather than deleted."
    - "The toolbar remains reachable when all groups are hidden, e1RM remains full-history, daily trend remains current 7/28/90 calendar days, and calendar history remains its own 12-week page."
    - "PROG-01 boundary: planned workout days remain 1–7 here; weekly muscle targets remain 1–40 in Plan 01-02; range cardinality is exactly 7/28/90."
    - "PROG-01 adjacency: range changes affect only range-sensitive summary, Daily, Body, and Balance calculations; e1RM and calendar paging retain independent semantics."
    - "PROG-01 empty: full, group-local, calendar-period, weighted-set, and zero-result empty states use UI-SPEC copy and do not hide unaffected analytics."
    - "PROG-01 ordering: exercise choices are alphabetical, series/daily values are chronological, and normalized saved group order is stable."
    - "PROG-01 precision: existing analytics formulas, dominant-unit conversion, and established rounding remain unchanged."
  artifacts:
    - path: src/screens/ProgressScreen.jsx
      provides: "Screen-owned four-group composition, toolbar, customization state, e1RM group, and hydration shell"
      contains: "Estimated one-rep max"
    - path: src/progressDashboardSettings.js
      provides: "Importable plain-JavaScript legacy five-card normalization and semantic no-op detection"
      exports: ["normalizeDashboardSettings", "updateDashboardSettings"]
    - path: src/progressDashboardSettings.test.js
      provides: "Node regression coverage for persisted-shape discrimination, legacy order/visibility migration, normalized hidden-Trend round trips, idempotency, and same-value no-op behavior"
      contains: "normalizeDashboardSettings"
    - path: src/components/SegmentedButtons.jsx
      provides: "Backward-compatible per-option ariaLabel forwarding for exact visible/accessibility label pairs"
      contains: "option.ariaLabel"
    - path: src/ProgressDashboard.jsx
      provides: "Subordinate Daily trend, Body heatmap, and Balance group implementations using existing pure analytics"
      contains: "Daily trend"
    - path: src/screens/ProgressScreen.css
      provides: "Adjacent phone-first Progress layout and group styles"
      contains: ".progress-screen"
    - path: src/App.jsx
      provides: "Progress-only saveProgressPreferences adapter and Progress hydration wiring; shared saveAccountPrefs remains unchanged"
      contains: "saveProgressPreferences"
  key_links:
    - from: src/App.jsx
      to: src/screens/ProgressScreen.jsx
      via: "sessions, equipmentPrefs, Progress-only saveProgressPreferences, and addDashboardExercise props"
      pattern: "<ProgressScreen"
    - from: src/screens/ProgressScreen.jsx
      to: src/progressDashboardSettings.js
      via: "screen composition imports tested normalization/update helpers instead of embedding migration logic in JSX"
      pattern: "normalizeDashboardSettings"
    - from: src/screens/ProgressScreen.jsx
      to: src/ProgressDashboard.jsx
      via: "named DailyTrendGroup, BodyHeatmapGroup, and BalanceGroup composition"
      pattern: "DailyTrendGroup"
    - from: src/screens/ProgressScreen.jsx
      to: src/stats.js
      via: "dominantUnit and exerciseE1RMSeries stay pure and full-history"
      pattern: "exerciseE1RMSeries"
    - from: src/ProgressDashboard.jsx
      to: src/stats.js
      via: "existing range, calendar, trend, muscle, and balance selectors"
      pattern: "dashboardRangeSummary"
  prohibitions:
    - id: PROHIB-PROG-SILENT-REMOVAL
      requirement: PROG-01
      category: transparency
      status: resolved
      verification: judgment
      enforcement: unverified-prohibition
      statement: "Visual consolidation must not silently remove any existing Progress analytic, detail, control, caution, suggestion, or callback recorded in the Claude progress ledger."
---

<objective>
As a workout tracker user, I want one coherent four-group Progress dashboard whose strength and day-by-day analytics retain their existing meaning, so that I can review progress and customize the dashboard without losing information.

Purpose: Establish the production four-group tracer through the current screen, preference callback, pure analytics, and shared Material primitives, then complete the e1RM and Daily trend vertical slices.
Output: A screen-owned four-group composition with a durable toolbar/customization path, complete e1RM group, and complete Daily trend/calendar group.
</objective>

<execution_context>
@/Users/pagraw15/.codex/gsd-core/workflows/execute-plan.md
@/Users/pagraw15/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-progress-completion/01-UI-SPEC.md
@../docs/superpowers/specs/2026-08-14-android-redesign-design.md
@.superpowers/sdd/2026-08-14-android-redesign-1-foundation/progress.md
@src/App.jsx
@src/screens/ProgressScreen.jsx
@src/screens/ProgressScreen.css
@src/ProgressDashboard.jsx
@src/ProgressDashboard.css
@src/stats.js
@src/trainingInsights.js
@src/dateUtils.js
@src/components/index.js
@src/charts/chartTheme.js
@src/charts/useThemeTokens.js
</context>

<threat_model>
ASVS L1 / block-high review: this plan changes presentation composition and the existing local preference callback only. It adds no authentication, network, import, workout-data, or cloud-authorization boundary, so it introduces no new high-risk security surface. Preserve the owner-scoped Firebase mirror and local-first storage path exactly. The material preservation risks are malformed persisted session shapes causing a group render failure, a failed local preference write being shown as confirmed, stale rapid preference events clobbering order/range, and visual consolidation dropping health-related analytics or notes. Contain failures at the Progress group boundary, confirm a preference only after `savePrefs` succeeds, and never write sessions or analytics results from this screen.
</threat_model>

## Artifacts this phase produces

- New files: `src/progressDashboardSettings.js` and `src/progressDashboardSettings.test.js`.
- New symbols in `src/progressDashboardSettings.js`: `DASHBOARD_KEY`, `PROGRESS_GROUP_IDS`, `PROGRESS_GROUP_LABELS`, `normalizeDashboardSettings`, and `updateDashboardSettings`.
- New/renamed symbols in `src/screens/ProgressScreen.jsx`: `ProgressToolbar` and `E1RMGroup`; normalization is imported from the plain-JavaScript module rather than defined in JSX.
- New App-local symbol: `saveProgressPreferences`; existing `saveAccountPrefs` and all of its completed Settings/goal/template callers remain unchanged.
- Expanded existing `SegmentedButtons` symbol: each option may provide `ariaLabel`; the button uses `option.ariaLabel || option.label`, preserving all existing callers by default.
- New/renamed symbols exported from `src/ProgressDashboard.jsx`: `DailyTrendGroup`, `BodyHeatmapGroup`, and `BalanceGroup`; these are extractions of current `ProgressDashboard` content, not new analytics.
- New CSS class families: `progress-toolbar`, `progress-group`, `progress-e1rm-*`, `progress-trend-*`, and `progress-calendar-*` in the existing Progress stylesheets.

<tasks>

<task type="tracer">
  <name>Task 1: Prove the screen-owned four-group and preference path</name>
  <reversibility rating="costly">The synced `cardOrder`/`hiddenCards` values change from five legacy card IDs to four group IDs; rollback requires keeping the normalization seam compatible across stored local/cloud preferences.</reversibility>
  <files>src/App.jsx, src/components/SegmentedButtons.jsx, src/progressDashboardSettings.js, src/progressDashboardSettings.test.js, src/screens/ProgressScreen.jsx, src/screens/ProgressScreen.css, src/ProgressDashboard.jsx, src/ProgressDashboard.css</files>
  <read_first>src/App.jsx, src/components/SegmentedButtons.jsx, src/progressDashboardSettings.js, src/progressDashboardSettings.test.js, src/screens/ProgressScreen.jsx, src/screens/ProgressScreen.css, src/ProgressDashboard.jsx, src/ProgressDashboard.css, src/equipmentPrefs.js, src/components/Sheet.jsx, .planning/phases/01-progress-completion/01-UI-SPEC.md, .superpowers/sdd/2026-08-14-android-redesign-1-foundation/progress.md</read_first>
  <action>Refactor the current boundary without changing analytics: make `ProgressScreen` own the global toolbar and ordered group composition, and extract the existing dashboard sections into `DailyTrendGroup`, `BodyHeatmapGroup`, and `BalanceGroup`. Create importable plain-JavaScript `src/progressDashboardSettings.js` and colocated Node tests. Export the existing-key constants plus `normalizeDashboardSettings(preferences)` and `updateDashboardSettings(settings, changes)`. Continue using `__dashboardSettings` with `rangeDays`, `plannedDays`, `targets`, `hiddenCards`, and `cardOrder`; do not create a second preference schema. Define the persisted-shape discriminator explicitly: an array `cardOrder` containing `e1rm` is already normalized; an absent/non-array `cardOrder` or one without `e1rm` is legacy. On the normalized path, filter/deduplicate only the four group IDs, append missing group IDs in authoritative default order, and preserve `hiddenCards: ["trend"]` as a hidden top-level Daily trend group. Apply the legacy `summary`/`calendar`/`trend` collapse only when `e1rm` is absent: place one `trend` at the earliest legacy subordinate position, map `heatmap` and `balance`, insert missing `e1rm` first, deduplicate, and append missing groups. Only on that legacy path is Daily trend hidden when all three legacy subordinate IDs are hidden. `updateDashboardSettings` returns the prior settings object for a semantic same-value change so the screen can skip persistence. Tests must cover all five legacy IDs, duplicates and missing IDs, missing `cardOrder` as legacy, earliest subordinate placement, the legacy all-three Daily-hidden rule, repeated normalization deep equality, same-value reference/no-save behavior, and the exact normalized round trip `{ cardOrder: ["e1rm", "trend", "heatmap", "balance"], hiddenCards: ["trend"] }` remaining hidden and deeply equal after repeated normalization. Render `Card` groups in saved order and keep the toolbar plus exact all-hidden copy/action when none are enabled. Extend `SegmentedButtons` additively: forward optional `option.ariaLabel` to each button's `aria-label`, defaulting to `option.label` for every completed caller. Pass visible/accessibility pairs `7 days`/`Show last 7 days`, `28 days`/`Show last 28 days`, and `90 days`/`Show last 90 days`. Add an App-local `saveProgressPreferences(updated)` adapter that uses the same existing `savePrefs(storage, activeProfile, updated)` path/schema: return `false` without confirmed Progress state or cloud update on local failure; on success update `equipmentPrefs`, preserve the optional Firebase mirror, and return `true`. Pass only this adapter to `ProgressScreen`; do not edit shared `saveAccountPrefs` semantics or any completed Settings, goal, template, or equipment caller. In Progress, keep the latest confirmed normalized settings ref/state so rapid actions derive from the latest confirmation; on `false`, retain prior settings, keep Customize open when applicable, and announce `Dashboard changes couldn’t be saved. Try again.`. For a saved active Progress tab during hydration, render the Progress shell and static tonal group skeletons with `Loading progress…`. Do not touch Home, Session, History, Weight, workout records, pure analytics, or Firebase schemas.</action>
  <verify>Run `node --test src/progressDashboardSettings.test.js` and `npm run build`. Source-assert the discriminator `Array.isArray(raw.cardOrder) && raw.cardOrder.includes("e1rm")`; that legacy collapse occurs only on its false branch; and tests for missing order plus the exact normalized `cardOrder`/hidden `trend` round trip and repeated deep equality. Also assert exactly four normalized IDs; the visible/accessibility range pairs; `option.ariaLabel || option.label`; `Customize dashboard`; the save error; `ProgressScreen` receiving `saveProgressPreferences`; unchanged pre-existing `saveAccountPrefs` callers; and removal of the old five-ID JSX constant.</verify>
  <acceptance_criteria>
    - `ProgressScreen` is the owner of the range control, Customize Sheet, saved group visibility, and saved group order.
    - The normalized settings use the current `__dashboardSettings` object and are idempotent; no new storage key or preference schema is introduced.
    - Plain-JavaScript Node tests prove five-ID migration, duplicate/missing repair, missing-order legacy handling, earliest Daily placement, legacy all-three hidden semantics, normalized hidden-Trend round-trip preservation, repeated equality, and same-value no-op behavior.
    - Segmented range options render exact visible labels and distinct accessible names while every existing caller retains label fallback behavior.
    - Only `saveProgressPreferences` uses failure-confirmation semantics; `saveAccountPrefs` and completed callers are unchanged.
    - Exactly four top-level `Card` groups are possible and legacy range summary/calendar/trend content is subordinate to Daily trend.
    - Repeating the same visibility/range action is idempotent, rapid reorder actions use the latest confirmed order, and a failed device save retains the prior confirmed setting and live error message.
    - The Progress hydration path displays static tokenized skeletons without briefly showing the no-session empty state.
  </acceptance_criteria>
  <done>A runnable user path exists from Progress toolbar to immediate durable range/customization changes and four ordered groups, with legacy preferences migrated in place and no analytic content removed.</done>
</task>

<task type="auto">
  <name>Task 2: Complete the full-history e1RM group</name>
  <files>src/screens/ProgressScreen.jsx, src/screens/ProgressScreen.css, src/ProgressDashboard.jsx</files>
  <read_first>src/screens/ProgressScreen.jsx, src/screens/ProgressScreen.css, src/ProgressDashboard.jsx, src/stats.js, src/trainingInsights.js, src/charts/chartTheme.js, src/charts/useThemeTokens.js, .planning/phases/01-progress-completion/01-UI-SPEC.md, ../docs/superpowers/specs/2026-08-14-android-redesign-design.md</read_first>
  <action>Finish `E1RMGroup` as the raised first analytics group. Preserve `exerciseE1RMSeries(sessions, exercise)`, `dominantUnit(sessions)`, current unit conversion/rounding, and the full history regardless of the global range. Keep eligible logged exercise names in stable `localeCompare` alphabetical order and series dates chronological. Add the exact eyebrow/heading, latest Headline value, first/latest/change text summary, unit-aware axes/tooltip, visible points, zero-safe domain, `connectNulls={false}`, concise chart label, and an accessible expandable list/table containing every source date/value/unit. Move the existing `trainingInsights(list)` output and evidence under this group exactly once, preserving all entries, Next step actions, recovery/plateau wording, evidence, wrapping, and `Trend-based guidance, not medical advice.`. Implement the exact weighted-set empty copy and an isolated retryable group error surface; zero, one, and many insights must read correctly. Do not change `estimated1RM`, `exerciseE1RMSeries`, `trainingInsights`, their tests, or persisted data.</action>
  <verify>Run `npm test` and `npm run build`; source-assert `exerciseE1RMSeries`, `trainingInsights`, `No weighted sets for`, `Log weight and reps to start this strength trend.`, `Trend-based guidance, not medical advice.`, `connectNulls={false}`, and an accessible e1RM data representation in the group.</verify>
  <acceptance_criteria>
    - Changing 7/28/90 does not change e1RM source length or dates; the line remains full-history.
    - Latest/first/change text and accessible values expose chart meaning without relying on hover or color.
    - The complete insight list appears once and no former insight, evidence token, action, or disclaimer is lost.
    - Empty and calculation-error states are local to e1RM and leave the other enabled groups usable.
  </acceptance_criteria>
  <done>The e1RM group is a complete, accessible, full-history vertical slice using the existing formulas and palette hook.</done>
</task>

<task type="auto">
  <name>Task 3: Complete Daily trend and independent 12-week calendar</name>
  <files>src/screens/ProgressScreen.jsx, src/screens/ProgressScreen.css, src/ProgressDashboard.jsx, src/ProgressDashboard.css</files>
  <read_first>src/screens/ProgressScreen.jsx, src/screens/ProgressScreen.css, src/ProgressDashboard.jsx, src/ProgressDashboard.css, src/stats.js, src/dateUtils.js, src/charts/useThemeTokens.js, .planning/phases/01-progress-completion/01-UI-SPEC.md, .superpowers/sdd/2026-08-14-android-redesign-1-foundation/progress.md</read_first>
  <action>Consolidate the existing range summary, daily chart, training calendar, consistency measures, and selected-day details inside the one `Daily trend` Card in that hierarchy. Keep `chartExercise`, `chartMetric`, `historyPage`, and `selectedDate` transient. Derive daily records from `todayISO()` independently of calendar page: `Array.from({ length: rangeDays })` must yield exactly 7/28/90 chronological ISO dates including zero days, with no bucket/sampling/omission. Keep All exercises plus Volume, Max weight, Estimated 1RM, Sessions and existing calculations/rounding. Add exact local zero-result copy, full-date/metric/value/unit tooltip, tick thinning only, and an accessible expandable table/list with every zero day. Preserve sessions, total volume, working sets, planned workout days, and the editable 1–7 planned-days target. Keep calendar `periodEnd`, `activityCalendar(..., 12, periodEnd)`, Earlier/Later/Current, consistency values, populated-day selection, all saved workout details, notes, exercise/set counts, and singular/plural wording separately paged. Give each populated day and navigation control a 48px target; place all 12 weeks in a labeled contained horizontal scroller instead of shrinking day targets. Closing details returns focus to the invoking date and does not jump scroll. Keep current pure stats/date functions unchanged.</action>
  <verify>Run `npm test`, `npm run test:tz`, and `npm run build`; run a Node source assertion that the daily record constructor uses `length: rangeDays`, calendar construction still uses 12 weeks and its own end date, and the metric option strings plus exact Daily zero-state copy exist.</verify>
  <acceptance_criteria>
    - Daily chart data has exact cardinality 7, 28, or 90 and includes a zero record for every calendar day without a workout.
    - Calendar paging cannot change the daily date range, Body/Balance range, or full-history e1RM.
    - Calendar empty periods remain navigable within valid history bounds and populated dates expose every available detail field.
    - Exercise choices are alphabetical; daily/calendar dates are chronological; formulas and display precision match the current implementation.
    - At 360px, selectors stack and the page never scrolls horizontally; only the labeled calendar container may scroll horizontally.
  </acceptance_criteria>
  <done>Users can inspect exact individual daily trends and separately page the preserved 12-week calendar within one subordinate-rich Daily trend group.</done>
</task>

</tasks>

<verification>

- [ ] `npm test` passes with the existing analytics suite unchanged.
- [ ] `npm run test:tz` passes in all configured time zones.
- [ ] `npm run build` succeeds.
- [ ] `npm run lint` introduces no findings in Progress-touched code; the already-recorded guest bootstrap `react-hooks/set-state-in-effect` concern in `App.jsx` is not broadened into Phase 1 cleanup.
- [ ] Source assertions prove four group IDs, exact 7/28/90 daily construction, separate 12-week paging, and all preserved analytics/copy named above.

</verification>

<success_criteria>

- All tasks completed and verification checks pass.
- PROG-01/02/03 have a real tracer through the existing preferences and analytics boundaries.
- e1RM is full-history, Daily is exact-current-range, calendar is separately paged, and no subordinate analytic is lost.
- No global legacy cleanup, deployment, push, new dependency, data-schema change, or pure analytics rewrite is included.

</success_criteria>

<output>
After completion, create `.planning/phases/01-progress-completion/01-01-SUMMARY.md`.
</output>
