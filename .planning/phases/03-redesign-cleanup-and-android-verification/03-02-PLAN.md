---
phase: 03-redesign-cleanup-and-android-verification
plan: "02"
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified: []
autonomous: false
requirements:
  - ARCH-03
  - VERI-01
  - VERI-02
  - VERI-03
user_setup: []
must_haves:
  truths:
    - "Home, History, Progress, Weight, Settings, and the session screen are usable at 360px and 390px in both light and dark themes, with no horizontal scroll and no stale-theme colours."
    - "Android-specific behaviour is confirmed on-device or in the Capacitor emulator: safe-area insets, theme-aware status bar colour, hardware back at every screen (including mid-session), Google sign-in, and rest-timer notifications."
    - "Full automated gate (tests, timezones, build, lint baseline) passes with no regression from Phase 3's code changes."
  artifacts: []
  key_links: []
---

<objective>
Close the redesign: run the full automated gate once more after 03-01's extraction, then a single human verification pass across every destination and the Android-specific behaviours the roadmap calls for.

Purpose: confirm nothing regressed during cleanup and that the app is genuinely ready to ship as the primary experience.
Output: a pass/fail checklist and, on approval, a version bump and deploy.
</objective>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/phases/03-redesign-cleanup-and-android-verification/03-01-SUMMARY.md
</context>

<threat_model>
ASVS L1. Verification only — no code change unless a defect is found, in which case fix it minimally and re-run the automated gate before returning to the checklist.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Full automated gate</name>
  <files></files>
  <read_first>package.json, scripts/verify-lint-baseline.mjs</read_first>
  <action>Run the complete automated gate and fix only what it reveals: npm test, npm run test:tz, npm run build, node scripts/verify-lint-baseline.mjs. Also grep src/App.jsx and every src/screens/*.jsx for any remaining style={{, hardcoded colour, or !important — there should be none left after 03-01.</action>
  <verify><automated>npm test && npm run test:tz && npm run build && node scripts/verify-lint-baseline.mjs && ! grep -rn "style={{" src/App.jsx src/screens/*.jsx</automated></verify>
  <done>All automated gates pass with zero inline styles anywhere in App.jsx or the screens.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Device and cross-theme verification</name>
  <files></files>
  <action>Start the dev server on the strict port for a browser-based 360/390px + light/dark pass, and separately confirm the Android-specific items on a device or in the Capacitor emulator (npm run android:run). Do not deploy, push, or bump the version until this checkpoint is approved.</action>
  <what-built>The complete redesigned app: Home, History, Progress, Weight, Settings, and the extracted session screen.</what-built>
  <how-to-verify>Browser at 360px and 390px, Light and Dark: visit all five nav destinations plus start-a-session, confirm no horizontal scroll, no stale colours crossing a theme switch, and 48px targets throughout. On an Android device or emulator: confirm the status bar colour matches the current theme, content respects safe-area insets (no content under the notch/gesture bar), the hardware back button works correctly from every screen and mid-session, Google sign-in completes, and a rest-timer notification fires when the app is backgrounded.</how-to-verify>
  <resume-signal>Type `approved` or describe concrete defects to fix.</resume-signal>
  <verify><automated>curl -fsS http://127.0.0.1:5173/</automated></verify>
  <done>Human approves both browser and Android verification, after any reported defect is fixed and the automated gate re-run.</done>
</task>

</tasks>

<verification>
- [ ] All automated gates green after 03-01's changes.
- [ ] Human approves 360/390px + light/dark across every destination.
- [ ] Human approves the five Android-specific behaviours.
</verification>

<success_criteria>
- ARCH-03, VERI-01, VERI-02, VERI-03 are satisfied.
- The redesign milestone is ready to close.
</success_criteria>

<output>After completion, create `.planning/phases/03-redesign-cleanup-and-android-verification/03-02-SUMMARY.md` (short).</output>
