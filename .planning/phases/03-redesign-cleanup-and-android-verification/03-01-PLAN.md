---
phase: 03-redesign-cleanup-and-android-verification
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/App.jsx
  - src/screens/SessionScreen.jsx
  - src/screens/SessionScreen.css
  - src/index.css
autonomous: true
requirements:
  - ARCH-02
user_setup: []
must_haves:
  truths:
    - "ARCH-02: SessionScreen owns active-workout presentation (day switcher, coach note, warm-up, exercise cards, set rows, rest dock, exit/discard confirmations); App retains draft state, timers, and save/discard handlers."
    - "No hardcoded colour, no `!important`, no `style={{` remains in the extracted block; every var(--token) used exists in src/design/tokens.css."
    - "Legacy index.css rules made dead by the extraction (.workout-card, .day-switcher, .rest-dock, .exercise-head, .exercise-actions, .set-row and their media variants, plus the session-exit-confirm styling) are removed; the reduced-motion rule at the end of the file is NOT touched."
    - "No behaviour change: draft autosave, rest timer, exit/discard confirmation, hardware back (popstate) during a session, and save-on-finish all work exactly as before."
  artifacts:
    - path: src/screens/SessionScreen.jsx
      provides: "Controlled active-workout screen: day switcher, coach note, warm-up, exercise cards, set entry, rest dock, exit/discard confirmations"
      contains: "SessionScreen"
    - path: src/screens/SessionScreen.css
      provides: "Tokenized session presentation, 360/390 responsive, no !important"
      contains: ".session-screen"
    - path: src/App.jsx
      provides: "SessionScreen routing plus draft/timer state and save/discard callbacks; no session presentation JSX"
      contains: "<SessionScreen"
  key_links:
    - from: src/App.jsx
      to: src/screens/SessionScreen.jsx
      via: "draft, dayMeta, rest timer state, and save/discard/exit callbacks as props"
      pattern: "<SessionScreen"
---

<objective>
Finish the screen-extraction pattern established across Phase 1/2 by moving the one remaining inline block — the active workout session — into its own screen. This is the last source of inline styles and legacy `!important` CSS in the app.

Purpose: leave App.jsx as pure state/routing, matching every other destination.
Output: SessionScreen.jsx + .css, a shrunk App.jsx, and a shrunk index.css.
</objective>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@../docs/superpowers/specs/2026-08-14-android-redesign-design.md
@.superpowers/sdd/2026-08-14-android-redesign-1-foundation/progress.md
@src/App.jsx
@src/screens/HistoryScreen.jsx
@src/screens/HistoryScreen.css
@src/components/Card.jsx
@src/components/Button.jsx
@src/design/tokens.css
</context>

<threat_model>
ASVS L1 / block-high: presentation-only extraction, no new trust boundary. The block being moved handles the in-progress workout draft, which autosaves locally — do not change when or how draft.date/exercises/sets are written, do not alter the rest-timer countdown logic, and preserve the exact hardware-back (`popstate`) exit-confirmation flow. A regression here could lose an in-progress workout or make the Android back button exit the app mid-session.
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extract SessionScreen</name>
  <files>src/App.jsx, src/screens/SessionScreen.jsx, src/screens/SessionScreen.css</files>
  <read_first>src/App.jsx (the active-session block, roughly where `activeTab==="log" && sessionActive` is checked), src/screens/HistoryScreen.jsx, src/screens/HistoryScreen.css, src/components/Card.jsx, src/components/Button.jsx, src/components/TextField.jsx, src/design/tokens.css</read_first>
  <behavior>
    - "The exact JSX structure, copy, and interaction of the current active-session block is preserved: day switcher, coach note toggle, warm-up toggle, exercise cards with set rows, add/remove set, rest dock, exit-confirm, discard-confirm."
    - "All state (draft, confirmExitSession, restRunning/restSeconds/restTarget, sessionHistoryRef) and handlers (toggleSetDone, updateSet, saveSession, discardDraft, leaveSession, startRestTimer, stopRestTimer, switchDay, moveDraftExercise, removeDraftExercise) stay in App.jsx and are passed as props."
    - "No inline style={{...}} remains in the new component; every visual property is a class backed by a token."
  </behavior>
  <action>Move the JSX block currently rendered when the log tab is active with a session in progress into a new SessionScreen component. App.jsx keeps all the state and handlers listed above unchanged and passes them as props; it only stops rendering the JSX directly. Build SessionScreen.css by converting each inline style to a class using existing design tokens, following HistoryScreen.css's conventions (tokenized spacing/shape/colour, 360/390px media queries where the original had responsive behaviour, min 48px touch targets, nothing below 10.5px). Reuse existing primitives (Card, Button, TextField, SegmentedButtons) where the current markup is equivalent to one.</action>
  <verify><automated>npm run build && grep -c "&lt;SessionScreen" src/App.jsx && ! grep -c "style={{" src/screens/SessionScreen.jsx</automated></verify>
  <done>The active-session UI renders from SessionScreen.jsx with zero inline styles, and App.jsx contains no session presentation JSX.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Remove dead legacy CSS</name>
  <files>src/index.css</files>
  <read_first>src/index.css, src/screens/SessionScreen.css</read_first>
  <behavior>
    - "Every selector in index.css that only ever targeted the now-extracted markup (.workout-card, .day-switcher, .rest-dock, .exercise-head, .exercise-actions, .set-row, .session-exit-confirm, and their @media variants) is removed."
    - "The @media (prefers-reduced-motion: reduce) block at the end of the file is untouched."
    - "No className referenced anywhere in src/App.jsx or src/screens/*.jsx is left without a matching rule after deletion."
  </behavior>
  <action>Delete every index.css rule made dead by Task 1's extraction. Before deleting a selector, grep the whole src/ tree to confirm nothing still references that className — SessionScreen.css may intentionally keep a couple of class names for continuity, in which case leave those specific rules and move them into SessionScreen.css instead of deleting them. After cleanup, index.css should hold only the @import, the view-transition/keyframes block, and the reduced-motion guard.</action>
  <verify><automated>npm run build && npm test && node scripts/verify-lint-baseline.mjs</automated></verify>
  <done>index.css contains no rule for markup that no longer exists, and every remaining className in the app resolves to a rule somewhere.</done>
</task>

</tasks>

<verification>
- [ ] `npm test` passes at the same count as before this plan (no logic changed).
- [ ] `npm run build` succeeds; `node scripts/verify-lint-baseline.mjs` shows only the one pre-existing baselined finding.
- [ ] No `style={{`, colour literal, or `!important` remains in `src/screens/SessionScreen.jsx`/`.css`.
- [ ] Manual: start a workout, log a set, trigger the rest timer, back out with Android/browser back, resume a draft — all behave exactly as before.
</verification>

<success_criteria>
- ARCH-02 is complete: every destination is a dedicated screen; App.jsx is state/routing only.
- No deploy, push, version bump, or Android sync occurs in this plan.
</success_criteria>

<output>After completion, create `.planning/phases/03-redesign-cleanup-and-android-verification/03-01-SUMMARY.md` (short, ~10-15 lines).</output>
